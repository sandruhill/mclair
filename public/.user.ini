; Suppresses the "X-Powered-By: PHP/x.y.z" header (exact-version recon info
; for anyone probing the site) and hides error detail from any response
; that isn't caught by our own code first.
expose_php = Off
display_errors = Off
